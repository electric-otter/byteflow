function executeUserCode() {
    const code = document.getElementById('userInput').value; // Capture the user input

    try {
        // Execute the user-provided code
        eval(code);
    } catch (error) {
        console.error("Invalid word:", error);
    }
}
function math(variable)
  Math.ceil(4.9);
function rounds()
  Math.round(4.6);
function and(function)
  return function + function
function say(input)
  const outputElement =
document.getElementById('output');
  outputElement.TextContent = text
